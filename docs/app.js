'use strict';
const C=window.ETFCore,R=window.SurfRules,F=C.fmt,main=document.getElementById('main');
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const url=s=>/^https:\/\//.test(s||'')?esc(s):'#';
let funds=[],error='',answers={},selected=new Set(),category='all',query='',group='all',region='all',catalogView='basic';
try{funds=C.catalog(window.ETF_DATA)}catch(e){error='가격 자료를 읽지 못했어요. 학습 콘텐츠는 계속 이용할 수 있습니다.';}
const categories={equity:'주식',bonds:'채권',commodity:'금·원자재',theme:'테마·산업',cash:'금리형'};
const groups={kospi:'국내 대표지수',sp500:'미국 S&P500',nasdaq:'미국 나스닥100',krdividend:'국내 배당',usdividend:'미국 배당',government:'국내 국고채',shortbond:'단기채권',rates:'CD·KOFR 금리',gold:'금 선물',semiconductor:'반도체'};
const groupNotes={sp500:'같은 S&P500이라도 비용과 분배 정책이 달라요. 가격 차트는 분배금 재투자 성과가 아니에요.',nasdaq:'S&P500과 구성종목·업종 비중이 달라요. 미국 시장에 관심이 있다는 이유만으로 자동 후보에 포함하지 않아요.',shortbond:'단기채도 원금 보장 상품은 아니에요. 서로 다른 채권 지수를 따를 수 있어요.',rates:'CD와 KOFR는 서로 다른 금리예요. 합성 ETF는 예금이 아니며 거래상대방 위험이 있어요.',usdividend:'미국 배당주에 투자해요. 분배금뿐 아니라 가격·환율 변화도 함께 봐야 해요.'};
const questions=[['goal','어떤 것을 먼저 알아보고 싶나요?',[['growth','자산을 꾸준히 키우는 투자'],['income','분배금을 받는 투자'],['learn','아직 몰라요 · 시장부터 이해하기']]],['horizon','이 돈을 언제 사용할 예정인가요?',[['short','1년 이내'],['medium','1년 이후 ~ 3년'],['long','3년보다 나중에']]],['risk','투자금이 줄어들 수 있다는 점은 어떤가요?',[['none','원금 손실은 받아들이기 어려워요'],['cautious','손실 가능성은 알지만 변동이 부담돼요'],['accept','큰 하락도 가능하다는 점을 이해해요']]],['market','어느 시장이 궁금한가요?',[['any','아직 정하지 않았어요'],['korea','국내 시장'],['us','미국 시장']]]];
const terms=[['etf','ETF는 어떤 상품인가요?','여러 자산을 담은 펀드를 거래소에서 주식처럼 사고팔 수 있는 상품이에요. 무엇을 담는지에 따라 위험도 달라져요. 여러 종목을 담아도 원금은 보장되지 않아요.'],['index','대표지수를 따라간다는 뜻','정해진 종목과 비중으로 계산하는 지수의 성과를 따라가려는 거예요. KOSPI200과 S&P 500은 서로 다른 시장을 대표하므로 수익률 차이를 운용 능력 차이로 해석하지 않아요.'],['fee','총보수와 실제 부담 비용','총보수는 운용·관리 등에 드는 정기적인 비용이에요. 기타비용과 매매비용 등이 더 발생할 수 있어요. 연 0.1%는 100만원에 단순 적용하면 1,000원이지만 실제 비용은 자산가치 등에 따라 달라져요.'],['distribution','분배금이 많으면 더 좋은가요?','지급액만으로 판단하기 어려워요. 분배금 지급에 따른 가격 변화도 함께 봐야 해요. 지급 여부와 금액은 바뀔 수 있고, 분배금을 받아도 전체 투자에서는 손실이 날 수 있어요.'],['risk','낮은 변동성은 원금 보장일까요?','아니에요. 채권 ETF도 금리 상승 등으로 가격이 떨어질 수 있어요. 과거 최대낙폭은 관측 기간의 고점 대비 가장 큰 하락이며, 미래 손실의 상한이 아니에요.'],['currency','미국 ETF인데 원화로 사요?','국내 상장 해외 주식 ETF를 원화로 거래할 수 있어요. 환헤지하지 않은 상품은 해외 주가와 환율 변화가 함께 원화 성과에 반영돼요.'],['price','가격 수익률과 총수익률','가격 수익률은 시작과 끝 가격의 변화예요. 분배금 재투자까지 포함한 성과와 구분해야 해요. 이 사이트는 공급자 종가를 사용하며 과거 가격 조정 여부를 독립 검증하지 않았어요.'],['trade','매수 전에 확인할 것','같은 지수를 추종하는지, 자료가 언제 기준인지, 보수와 추가 비용은 무엇인지 살펴봐요. 거래량만으로 유동성을 판단하지 말고 실제 매수·매도 호가 차이와 NAV 대비 가격도 확인해요.']];
terms.push(['nasdaq','S&P500과 나스닥100은 어떻게 다른가요?','두 지수의 구성종목과 업종 비중은 달라요. 나스닥100은 기술·성장 기업의 영향을 크게 받을 수 있어요. 두 상품을 함께 보유해도 같은 종목이 겹칠 수 있으니 이름보다 구성을 확인해요.'],['bonds','단기채는 예금처럼 안전한가요?','만기가 짧아도 금리와 신용 위험이 있어요. 상품마다 국고채·통안채 등 투자 대상과 지수가 다릅니다. 단기채와 국고채3년 ETF의 금리 민감도도 같지 않아요.'],['cash','CD·KOFR 금리형은 무엇인가요?','단기 금리를 비교기준으로 삼는 ETF예요. CD와 KOFR는 서로 다른 금리이며 미래 수익은 고정되어 있지 않아요. 합성 상품은장외파생상품을 활용하므로 거래상대방 위험도 살펴봐야 해요. 은행 예금과 달리 예금자보호나 원금 보장이 없습니다.']);
function heading(k,t,p=''){return `<p class="eyebrow">${k}</p><h1>${t}</h1><p class="muted">${p}</p>`}
function updateNotice(){const s=window.ETF_UPDATE;if(!s||s.state==='not_run')return '자동 수집 실행 이력이 없습니다.';const when=Number.isFinite(Date.parse(s.attemptedAt))?new Date(s.attemptedAt).toLocaleString('ko-KR',{timeZone:'Asia/Seoul'}):'확인 불가';return (s.state==='failed'?'최근 갱신 실패 · 마지막 정상 가격을 표시합니다.':'최근 수집 성공 · 휴장일에는 가격 기준일이 그대로일 수 있습니다.')+' 확인 '+esc(when)+' (한국시간) · <a href="https://github.com/wjyi0615/etf-surf/actions">갱신 기록 ↗</a>';}
function notice(){return error?`<p class="notice warning" role="alert">${error}</p>`:`<p class="notice">가격 ${funds[0]?.asOf} 기준 · 저장된 자료이며 실시간 시세가 아닙니다.${Date.now()-Date.parse(funds[0]?.asOf)>7*864e5?' 최근 7일 이상 지난 자료입니다.':''} 기본 정보의 기준일은 항목마다 다릅니다.<br>${updateNotice()}</p>`}
function fundamental(e,key){const r=e.metadata[key];if(!r)return '미확보';const v=key==='aum'?F.number(Math.round(r.value/1e8))+'억원':key==='expenseRatio'?'연 '+(r.value*100).toFixed(4)+'%':esc(r.value);return `${v}<small>${r.asOf?esc(r.asOf)+' 자료':'자료 기준일 미표기'}${r.asOf&&Date.now()-Date.parse(r.asOf)>31*864e5?' · 과거 자료':''}</small><small><a href="${url(r.sourceUrl)}" target="_blank" rel="noopener noreferrer">공식 출처 ↗</a> · 확인 ${esc(r.checkedAt)}</small>`}
function button(e){return `<button data-select="${e.ticker}" aria-pressed="${selected.has(e.ticker)}">${selected.has(e.ticker)?'비교에서 제외':'비교에 담기'}</button>`}
function bar(){return selected.size?`<div class="compare-bar"><span>${selected.size}개를 담았어요 · 최대 3개</span><a href="#/compare">비교하러 가기 →</a></div>`:''}
function card(e){const x=R.explanation(e);return `<article class="card"><span class="badge">${categories[e.category]} · ${e.region==='us'?'미국':e.region==='global'?'글로벌':'국내'}</span><h3><a href="#/etf/${e.ticker}">${esc(e.name)}</a></h3><small>${e.ticker} · ${esc(e.issuer)}</small><p>${esc(e.description)}</p><details><summary>왜 살펴볼까요?</summary><p><b>선정 이유</b> · ${esc(x.why)}</p><p class="risk"><b>주요 위험</b> · ${esc(x.risk)}</p><p><b>확인할 지표</b> · ${esc(x.check)}</p></details><small>추종지수 · ${esc(e.benchmark)}</small><div class="actions">${button(e)}<a href="#/etf/${e.ticker}">자세히 →</a></div></article>`}
function home(){return `<section class="hero"><div><p class="eyebrow">UNDERSTAND FIRST. SURF NEXT.</p><h1>첫 ETF,<br>나만의 방향을<br>찾아보세요.</h1><p class="muted">수익률 숫자보다, 무엇에 투자하는지부터.<br>몇 가지 질문으로 살펴볼 ETF를 찾고<br>선택의 이유를 차근차근 이해해요.</p><div class="actions"><a class="button" href="#/guide">나에게 맞는 ETF 유형 알아보기 ↗</a><a class="button secondary" href="#/explore">먼저 둘러보기</a></div><p><small>로그인 없이 · 답변은 서버에 저장하지 않아요</small></p></div><div class="ocean" aria-hidden="true"><svg viewBox="0 0 480 400" preserveAspectRatio="none"><circle cx="354" cy="78" r="39" fill="#f1d39a"/><path d="M0 180 Q120 80 240 180 T480 180 V400 H0Z" fill="#96bcb7"/><path d="M0 270 Q120 160 240 270 T480 270 V400 H0Z" fill="#4f8a89"/><path d="M0 335 Q120 230 240 335 T480 335 V400 H0Z" fill="#164b53"/></svg><div class="ticket"><span class="eyebrow">MY FIRST ETF</span><strong>이해하고,<br>비교하고,<br>선택하기.</strong><p>나의 속도로 시작하는 투자</p></div></div></section><div class="steps"><div><b>01 / FIND</b><h3>나의 방향 찾기</h3><p>투자 목적과 기간을 돌아봐요.</p></div><div><b>02 / COMPARE</b><h3>차이를 이해하기</h3><p>비용과 위험을 함께 비교해요.</p></div><div><b>03 / LEARN</b><h3>한 걸음 배우기</h3><p>궁금한 용어를 바로 알아봐요.</p></div></div><section class="section"><div class="section-head"><h2>어떤 바다를 살펴볼까요?</h2><a href="#/explore">전체 보기 →</a></div><div class="grid">${Object.entries(categories).map(([k,v])=>`<a class="card" href="#/explore/${k}"><span class="eyebrow">${k.toUpperCase()}</span><h3>${v}</h3><p>${{equity:'여러 기업의 성장에 함께하는 투자',bonds:'금리와 채권 가격의 관계 이해하기',commodity:'현물과 선물의 차이부터 알아보기',theme:'한 산업에 집중할 때의 기회와 위험',cash:'CD·KOFR 금리와 합성 구조 알아보기'}[k]}</p></a>`).join('')}</div></section>`}
function guide(){return heading('FIND YOUR DIRECTION','어디로 떠나고 싶나요?','정답은 없어요. 네 가지 답변으로 살펴볼 후보를 좁혀볼게요.')+`<form id="guide" class="panel"><div class="form-grid">${questions.map(([key,label,opts],i)=>`<fieldset><legend>${i+1}. ${label}</legend><p class="muted">${{goal:"관심 있는 투자 방식을 이해하기 위한 질문이에요.",horizon:"가까운 시일에 쓸 돈인지 먼저 확인해요. 투자 기간이 길어도 손실은 생길 수 있어요.",risk:"손실 가능성을 어떻게 느끼는지 돌아봐요. 이 답변만으로 위험 감수 능력을 판단하지 않아요.",market:"관심 시장을 반영해 학습 후보를 좁혀요."}[key]}</p>${opts.map(([value,text])=>`<label class="choice"><input type="radio" name="${key}" value="${value}" required ${answers[key]===value?'checked':''}><span>${text}</span></label>`).join('')}</fieldset>`).join('')}</div><div class="actions"><button type="submit">어떤 유형부터 볼지 알아보기 →</button><a href="#/method">후보 선정 기준</a></div><p class="muted">답변은 현재 열린 페이지에서만 사용해요. 새로고침하면 지워집니다. 개인별 투자 적합성을 판정하는 설문은 아닙니다.</p></form>`}
function scenarioBox(list){if(!list.length)return '';return `<section class="panel section"><h2>투자 시나리오를 살펴볼까요?</h2><p>과거 종가로 매달 같은 금액을 넣었다면 어떻게 보였는지 계산합니다. 미래 수익률이나 매수 권유가 아닙니다.</p><form id="scenario" class="form-grid"><label>ETF<select name="ticker">${list.map(e=>`<option value="${e.ticker}">${esc(e.name)}</option>`).join('')}</select></label><label>매월 투자금액 (원)<input name="monthly" type="number" min="1000" max="100000000" step="1000" value="100000" required></label><label>월 납입 횟수<select name="months"><option value="12">12회</option><option value="36">36회</option><option value="60">60회</option></select></label><div class="actions"><button type="submit">과거 결과 계산하기</button></div></form><div id="scenario-result" aria-live="polite"></div><p class="muted">정수 단위 매수와 잔여 현금만 반영합니다. 분배금·세금·거래비용은 계산하지 않으며 공급자의 과거 가격 조정 여부는 미검증입니다.</p></section>`}
function answerSummary(){return `<section class="panel section"><p class="eyebrow">01 / MY ANSWERS</p><h2>내 답변부터 돌아봐요</h2><dl class="answer-summary">${questions.map(([k,label,opts])=>`<div><dt>${esc(label)}</dt><dd>${esc(opts.find(([v])=>v===answers[k])?.[1])}</dd></div>`).join('')}</dl><div class="actions"><a href="#/guide">답변 바꾸기 →</a><a href="#/method">후보 선정 기준</a></div><p class="muted">이 답변으로 투자 적합성이나 안전성을 판정하지 않아요.</p></section>`;}
function understandingCheck(){return `<section class="panel section"><p class="eyebrow">BEFORE YOU DECIDE</p><h2>선택 전에, 세 가지를 설명할 수 있나요?</h2><p>헷갈리는 항목은 설명을 다시 읽어보세요. 모두 이해해도 손실 가능성이 사라지지는 않아요.</p><ul class="check-links"><li><a href="#/learn/etf">무엇에 투자하는 상품인지 알고 있나요? →</a></li><li><a href="#/learn/risk">어떤 이유로 손실이 날 수 있는지 알고 있나요? →</a></li><li><a href="#/learn/trade">비용과 자료 기준일을 확인했나요? →</a></li></ul></section>`;}
function result(){
 let r;try{r=R.recommend(answers,funds)}catch{return heading('START HERE','먼저 나의 방향을 알려주세요')+'<a class="button" href="#/guide">질문에 답하기 →</a>';}
 const intro=`<div class="result-head"><p class="eyebrow">YOUR NEXT STEP</p><h1>상품보다, 투자 유형부터 이해해요</h1><p>내 답변 → 유형 이해 → 후보 탐색 → 비교</p></div>`+answerSummary();
 if(r.kind!=='candidates')return intro+`<section class="panel section"><h2>${esc(r.title)}</h2><p>${esc(r.reason)}</p><p>${r.kind==='pause'?'지금 필요한 돈과 투자할 돈을 구분하고, ETF의 손실 가능성부터 알아봐요.':'조건에 맞는 상품이 등록되지 않았다는 뜻이에요. 시장에 상품이 없거나 투자할 수 없다는 뜻은 아니에요.'}</p><a class="button" href="#/learn">ETF 기초 배우기 →</a></section>`;
 const e=r.funds[0],x=R.explanation(e),lesson=e.category==='bonds'?'risk':e.strategy==='dividend'?'distribution':'index';
 const type=e.category==='bonds'?'국고채 ETF':e.strategy==='dividend'?'배당 전략 ETF':'대표지수 주식 ETF';
 const target=e.category==='bonds'?'국내 국고채에 투자하며 채권 가격과 금리의 관계를 살펴보는 유형이에요.':e.strategy==='dividend'?'배당 전략에 따라 주식을 담아요. 가격 변화와 지급받은 분배금을 함께 살펴봐요.':'KOSPI200이나 S&P 500처럼 여러 기업으로 구성된 지수를 따라가려는 유형이에요. 지수마다 투자 시장과 구성은 달라요.';
 const difference=e.category==='bonds'?'주식 ETF가 기업의 주가에 영향을 받는다면, 채권 ETF는 금리와 보유 채권의 특성을 살펴봐야 해요.':e.strategy==='dividend'?'대표지수형과 달리 배당 전략에 따라 종목을 골라요. 분배금이 많다는 이유만으로 더 좋은 성과는 아니에요.':'한 산업에 집중하는 ETF와 달리 여러 산업을 담는 지수를 살펴봐요. 여러 종목을 담아도 시장 하락 위험은 남아요.';
 const list=r.funds.slice(0,3);
 const peer=R.peers(e,list)[0];
 return intro+`<section class="panel section"><p class="eyebrow">02 / UNDERSTAND</p><h2>먼저 ${type}를 알아보세요</h2><p>${target}</p><h3>왜 이 유형부터 볼까요?</h3><p>${esc(r.reason)}</p><p class="risk"><b>손실이 날 수 있는 이유</b><br>${esc(x.risk)}${e.strategy==='dividend'?' 분배금의 지급 여부와 규모도 보장되지 않아요.':''}${r.funds.some(f=>f.region==='us')?' 미국 주식 상품은 환율 변화도 확인해야 해요.':''}</p><h3>다른 유형과 무엇이 다른가요?</h3><p>${difference}</p><a href="#/learn/${lesson}">관련 개념 더 알아보기 →</a></section>
 <section class="section"><p class="eyebrow">03 / EXPLORE</p><h2>이제 상품을 살펴볼까요?</h2><p>조건에 맞는 등록 상품 ${r.funds.length}개 중 ${list.length}개를 먼저 보여드려요. 종목코드 순서이며 우수 상품이나 추천 순위가 아닙니다.</p>${notice()}<div class="grid">${list.map(card).join('')}</div>${r.funds.length>3?`<details class="panel section"><summary>나머지 후보 ${r.funds.length-3}개도 보기</summary><div class="grid section">${r.funds.slice(3).map(card).join('')}</div></details>`:''}</section>
 <section class="panel section"><p class="eyebrow">04 / COMPARE</p><h2>처음에는 두 상품만 비교해요</h2>${peer?`<p>같은 ${esc(e.benchmark)} 지수를 따르는 ${esc(e.name)}과 ${esc(peer.name)}을 비교하며 비용과 자료 기준일을 읽어보세요.</p><button data-peer="${e.ticker}:${peer.ticker}">두 상품 비교하기 →</button><p class="muted">종목코드 순으로 고른 학습 예시예요. 기존 비교 선택을 이 두 상품으로 바꿉니다.</p>`:'<p>현재 후보에는 같은 지수·유형으로 비교할 두 상품이 없어요. 먼저 상품 구조와 위험을 읽어보세요.</p><a href="#/etf/'+e.ticker+'">후보 상세 이해하기 →</a>'}</section>
 <details class="panel section"><summary>이해했다면, 과거 투자 시나리오 체험하기</summary>${scenarioBox(r.funds)}</details>${understandingCheck()}`;
}
function filterSelect(name,label,options,value){return `<label>${label}<select name="${name}"><option value="all">전체</option>${Object.entries(options).map(([k,v])=>`<option value="${k}" ${k===value?'selected':''}>${v}</option>`).join('')}</select></label>`;}
/** Compact catalog; product links retain native keyboard and new-tab behavior. */
function catalogTable(list){
 const performance=catalogView==='returns';
 const labels=performance?['1개월','3개월','1년']:['최근 종가','총보수','1년 가격수익률'];
 return `<div class="catalog-views" aria-label="목록 표시 항목"><button data-view="basic" aria-pressed="${!performance}">기본정보</button><button data-view="returns" aria-pressed="${performance}">가격 수익률</button></div><p class="muted catalog-note">총보수는 항목별 기준일의 과거 자료이며 추가 비용이 발생할 수 있어요. 최신 비용·출처는 상품 상세에서 확인하세요.</p><div class="table-wrap catalog-table" tabindex="0" role="region" aria-label="ETF 상품 목록"><table><caption>상품명을 눌러 자세히 알아보세요.</caption><thead><tr><th scope="col">상품</th><th scope="col">특성</th>${labels.map(t=>`<th scope="col">${t}</th>`).join('')}<th scope="col">비교</th></tr></thead><tbody>${list.map(e=>{
 const values=performance?['month','quarter','year'].map(k=>F.percent(e.returns[k])):[F.money(e.price),Number.isFinite(e.expenseRatio)?'연 '+(e.expenseRatio*100).toFixed(4)+'%':'미확보',F.percent(e.returns.year)];
 return `<tr><th scope="row"><a class="product-name" href="#/etf/${e.ticker}">${esc(e.name)} →</a><small>${e.ticker} · ${esc(e.issuer)}</small></th><td class="product-tags"><span class="badge">${esc(categories[e.category])}</span><span class="badge">${esc(groups[e.group]||e.benchmark)}</span></td>${values.map((v,i)=>`<td class="numeric" data-label="${labels[i]}">${v}</td>`).join('')}<td class="compare-cell"><label class="compare-check"><input type="checkbox" data-select="${e.ticker}" ${selected.has(e.ticker)?'checked':''} aria-label="${esc(e.name)} 비교 선택"><span>비교</span></label></td></tr>`;
 }).join('')}</tbody></table></div><p class="muted catalog-note">공급자 종가의 가격 변화입니다. 분배금 재투자 수익률이 아니며 상품별 관측 기간이 다를 수 있어요.</p>`;
}
function explore(){
 const list=C.filterEtfs(funds,{category,query,group,region});
 return heading('EXPLORE','어떤 ETF가 있을까요?',`등록된 ${funds.length}개 상품을 유형과 시장으로 살펴봐요. 전체 시장이나 우수 상품 순위가 아닙니다.`)+notice()
 +`<div class="topic-links" aria-label="투자 유형 바로가기">${Object.entries(groups).map(([k,v])=>`<a href="#/explore/${k}">${v}</a>`).join('')}</div>${groupNotes[group]?`<p class="notice">${groupNotes[group]}</p>`:''}
 <form class="filters" id="filters">${filterSelect('category','종류',categories,category)}${filterSelect('group','세부 유형',groups,group)}${filterSelect('region','투자 시장',{korea:'국내',us:'미국',global:'글로벌'},region)}<label>상품 검색<input type="search" name="query" value="${esc(query)}" placeholder="상품명, 운용사, 지수, 종목코드"></label><button type="submit">찾기</button><a href="#/explore/all">필터 초기화</a></form>`
 +bar()+`<p class="muted">${list.length}개 상품 · 국내 상장 ETF · 상품명에서 상세 설명 보기</p>`+(list.length?catalogTable(list):'')
 +(!list.length?'<div class="empty"><h2>검색 결과가 없어요</h2><p>다른 검색어나 유형·시장을 선택해 보세요.</p></div>':'');
}
function chart(list){const series=R.comparison(list);if(!series.length)return '<p>선택한 상품 모두의 공통 1년 가격 이력이 충분하지 않습니다.</p>';const vals=series.flatMap(s=>s.p.values);let lo=Math.min(0,...vals),hi=Math.max(0,...vals);if(hi===lo)hi=lo+.01;const pad=(hi-lo)*.12;lo-=pad;hi+=pad;const x=i=>64+i*796/(series[0].p.values.length-1),y=v=>250-(v-lo)/(hi-lo)*220;const colors=['#166b75','#b56318','#7353a1'];return `<svg class="chart" viewBox="0 0 900 300" role="img" aria-label="최근 1년 공통 거래일의 가격 누적수익률">${[0,.25,.5,.75,1].map(t=>{const v=lo+(hi-lo)*t;return `<line x1="64" y1="${y(v)}" x2="860" y2="${y(v)}" stroke="#dfe7e2"/><text x="54" y="${y(v)+4}" text-anchor="end" font-size="12" fill="#586e71">${(v*100).toFixed(1)}%</text>`}).join('')}${series.map((s,j)=>`<polyline fill="none" stroke="${colors[j]}" stroke-width="2.5" points="${s.p.values.map((v,i)=>x(i).toFixed(1)+','+y(v).toFixed(1)).join(' ')}"/>`).join('')}<text x="64" y="280" font-size="12" fill="#586e71">${series[0].p.dates[0]}</text><text x="860" y="280" font-size="12" text-anchor="end" fill="#586e71">${series[0].p.dates.at(-1)}</text></svg><div class="legend">${series.map((s,i)=>`<span style="--color:${colors[i]}">${esc(s.e.name)}</span>`).join('')}</div><p class="muted">${series[0].p.dates[0]} ~ ${series[0].p.dates.at(-1)} · 공통 시작일 가격 = 0%. 분배금 재투자 성과가 아니며, 공급자 가격 조정 이력은 미검증입니다.</p>`}
function compare(){const list=funds.filter(e=>selected.has(e.ticker)),metrics=new Map(R.comparison(list).map(x=>[x.e.ticker,x.p]));return heading('SIDE BY SIDE','차이를 이해하는 비교','최대 3개를 같은 기준으로 살펴보세요.')+notice()+(!list.length?'<div class="empty panel"><h2>아직 담은 ETF가 없어요</h2><p>탐색 화면에서 비교에 담기를 눌러주세요.</p><a class="button" href="#/explore">ETF 둘러보기 →</a></div>':`<div class="actions">${list.map(e=>`<button class="secondary" data-select="${e.ticker}" aria-label="${esc(e.name)} 비교에서 제외">${esc(e.name)} ×</button>`).join('')}<a href="#/explore">다른 상품 담기</a></div><p class="notice warning">${new Set(list.map(e=>e.benchmark)).size>1?'서로 다른 지수·자산을 비교하고 있어요. 수익률 차이는 운용 능력 순위가 아닙니다.':'같은 지수를 추종해도 보수·분배금·거래가격은 달라질 수 있어요.'} 기본 정보는 서로 다른 기준일의 과거 자료이며 현재 비용·규모 순위를 의미하지 않습니다.</p><section class="panel"><h2>최근 1년 가격 흐름</h2>${chart(list)}<div class="table-wrap"><table><caption>선택한 ETF 비교</caption><thead><tr><th scope="col">항목</th>${list.map(e=>`<th scope="col"><a href="#/etf/${e.ticker}">${esc(e.name)}</a></th>`).join('')}</tr></thead><tbody>${[['운용사',e=>esc(e.issuer)],['추종지수',e=>esc(e.benchmark)],['총보수',e=>fundamental(e,'expenseRatio')],['순자산',e=>fundamental(e,'aum')],['최근 종가',e=>F.money(e.price)],['거래량',e=>F.number(e.volume)+'좌'],['1년 가격 수익률',e=>F.percent(metrics.get(e.ticker)?.total)],['1년 최대낙폭',e=>F.percent(metrics.get(e.ticker)?.mdd)],['분배금 내역',e=>e.distributions.length?'일부 확인 · 상세에서 조회':'미확보']].map(([k,f])=>`<tr><th scope="row">${k}</th>${list.map(e=>`<td>${f(e)}</td>`).join('')}</tr>`).join('')}</tbody></table></div><p class="muted">총보수에는 모든 거래 비용이 포함되지 않습니다. 거래량만으로 체결 품질을 판단할 수 없어요.</p></section>`)}
function detail(ticker){const e=funds.find(x=>x.ticker===ticker);if(!e)return heading('NOT FOUND','상품을 찾지 못했어요')+'<a href="#/explore">탐색으로 돌아가기</a>';const h=e.distributionHistory;return heading('ETF OVERVIEW',esc(e.name),`${e.ticker} · ${esc(e.issuer)}`)+notice()+`<p class="notice">${esc(e.description)}</p><div class="actions">${button(e)}<a href="#/compare">비교 보기 →</a><a href="${url(e.sourceUrl)}" target="_blank" rel="noopener noreferrer">상품 공식 자료 ↗</a></div>${nameGuide(e)}${detailGuide(e)}${composition(e)}<section class="panel section"><h2>상품 기본 정보</h2><dl>${[['유형',esc(e.productType)],['추종지수',esc(e.benchmark)],['최근 종가',F.money(e.price)]].map(([k,v])=>`<div><dt>${k}</dt><dd>${v}</dd></div>`).join('')}</dl><a href="#/learn/fee">보수 읽는 법 →</a></section><section class="panel section"><h2>최근 1년 가격 흐름</h2>${chart([e])}</section><section class="panel section"><h2>확인한 분배금 내역</h2>${h?`<p>전체 지급 이력이 아닌 일부 내역입니다. 세전 1좌당 금액이며 지급 기준일은 분배락일과 다릅니다.</p><p class="source">확인 ${esc(h.checkedAt)} · <a href="${url(h.sourceUrl)}" target="_blank" rel="noopener noreferrer">${esc(h.sourceName)} ↗</a></p><div class="table-wrap"><table><caption>${esc(e.name)} 일부 지급 내역</caption><thead><tr><th>지급 기준일</th><th>실제 지급일</th><th>1좌당 금액</th></tr></thead><tbody>${[...e.distributions].reverse().map(d=>`<tr><td>${d.recordDate}</td><td>${d.paymentDate}</td><td>${F.money(d.amountPerShare)}</td></tr>`).join('')}</tbody></table></div>`:'<p>분배금 자료 미확보. 지급액이 0원이라는 뜻은 아닙니다.</p>'}<p class="muted">이 내역으로 연간 분배율을 계산하지 않으며 가격 차트에도 합산하지 않습니다.</p><a href="#/learn/distribution">분배금 이해하기 →</a></section>${dataAvailability(e)}${similarProducts(e)}`}
/** Only a dated, validated fund-specific snapshot is rendered as holdings. */
/** Explain only terms present in the registered product name. */
function nameGuide(e){
 const terms=[['미국','미국 시장을 투자 대상으로 삼는다는 뜻이에요. 국내 상장 여부와 투자 지역은 달라요.'],['선물','현물을 직접 보유하는 대신 선물 계약을 활용해요. 계약 교체 비용 등으로 현물 수익률과 달라질 수 있어요.'],['(H)','환율 변동 영향을 줄이는 환헤지 전략을 뜻해요. 환율 영향을 완전히 없애거나 원금을 보장하지는 않아요.'],['합성','주로 거래 상대방과의 파생상품 계약으로 목표 수익률을 구현해요. 계약 상대방 위험도 확인해요.'],['액티브','비교지수 대비 초과성과 등을 목표로 운용 판단을 반영해요. 실제 운용 목표는 설명서에서 확인해요.'],['단기채권','만기가 짧은 채권에 투자해요. 금리와 발행자의 신용 위험이 남아 있어요.']];
 return `<section class="panel section"><h2>ETF 이름 풀어보기</h2><p><b>${esc(e.name.split(' ')[0])}</b>는 ${esc(e.issuer)}의 ETF 브랜드예요.</p><dl>${terms.filter(([term])=>e.name.includes(term)).map(([term,text])=>`<div><dt>${esc(term)}</dt><dd>${esc(text)}</dd></div>`).join('')}<div><dt>투자 기준</dt><dd>${esc(e.benchmark)}</dd></div></dl><small>이름은 첫 단서예요. 정확한 투자 대상과 운용 방식은 <a href="${url(e.sourceUrl)}" target="_blank" rel="noopener noreferrer">공식 상품 자료 ↗</a>에서 확인해요.</small></section>`;
}
/** Age uses the source date, never the date on which we checked the source. */
function compositionAge(h, today=new Date()){
 const age=Math.floor((today.getTime()-Date.parse(h?.asOf))/86400000);
 if(!Number.isFinite(age)||age<0)return '자료 기준일 확인 필요';
 return age>=180?'오래된 자료 · 기준일로부터 180일 이상 경과':'과거 자료 · 현재 구성과 다를 수 있어요';
}
function composition(e){
 const h=window.ETF_FUNDAMENTALS?.holdings?.[e.ticker];
 const valid=h&&/^\d{4}-\d{2}-\d{2}$/.test(h.asOf)&&Number.isFinite(Date.parse(h.asOf))&&h.asOf<=h.checkedAt&&/^https:\/\//.test(h.sourceUrl)&&h.complete===false&&Array.isArray(h.items)&&h.items.length>0&&h.items.every(x=>typeof x.name==='string'&&Number.isFinite(x.weight)&&x.weight>=0&&x.weight<=100)&&h.items.reduce((v,x)=>v+x.weight,0)<=100;
 const g=R.productGuide(e);
 const reading=`<div class="detail-reading"><h3>구성을 이렇게 읽어요</h3><p><b>투자 대상</b> · ${esc(g.target)}</p><p><b>영향 요인</b> · ${esc(g.drivers)}</p><p><b>주의할 점</b> · ${esc(g.check)}</p></div>`;
 const dated=h?`<p class="notice"><b>${esc(compositionAge(h))}</b><br>자료 기준일 ${esc(h.asOf)} · 출처 확인일 ${esc(h.checkedAt)}<br><small>180일은 이 사이트의 재확인 표시 기준입니다. 최근 자료도 현재 구성을 보장하지 않아요.</small></p>`:'';
 if(h?.kind==='structure'&&/^https:\/\//.test(h.sourceUrl)&&Number.isFinite(Date.parse(h.asOf)))return `<section class="panel section"><p class="eyebrow">INSIDE THE ETF</p><h2>이 ETF 안에는 무엇이 있나요?</h2>${dated}${reading}<p>${esc(h.note)}</p><p class="muted">실제 편입 비중표가 아닌 공식 자료의 투자 구조 설명입니다. 가격 갱신으로 이 자료가 자동 갱신되지는 않아요.</p><a href="${url(h.sourceUrl)}" target="_blank" rel="noopener noreferrer">구성 방식 공식 자료 ↗</a></section>`;
 return `<section class="panel section"><p class="eyebrow">INSIDE THE ETF</p><h2>이 ETF 안에는 무엇이 있나요?</h2>${reading}${dated}${valid?`<p class="notice">${esc(h.asOf)} 기준 과거 구성 · 현재 보유 내역이 아닙니다. 확인 ${esc(h.checkedAt)}</p><p>비중은 공식 자료에 표시된 구성 비율이에요. ${e.category==='commodity'?'선물 비중은 현물 보유 비중과 다르며 계약 교체가 필요해요.':e.category==='bonds'?'채권은 비중뿐 아니라 만기와 금리 민감도를 함께 봐야 해요.':'같은 비율로 주가가 움직여도 비중이 큰 종목의 영향이 더 클 수 있어요.'}</p><ol class="holdings-list">${h.items.map(x=>`<li><div><span>${esc(x.name)}</span><b>${x.weight.toFixed(2)}%</b></div><meter min="0" max="100" value="${x.weight}" aria-label="${esc(x.name)} 비중 ${x.weight}%">${x.weight}%</meter></li>`).join('')}</ol><p class="muted">공식 자료에서 확인한 ${h.items.length}개 항목입니다. 전체 구성은 아니며 종목과 비중은 바뀔 수 있어요. 가격 갱신과 별도로 확인한 자료입니다. ${esc(h.note||'')}</p><a href="${url(h.sourceUrl)}" target="_blank" rel="noopener noreferrer">${esc(h.sourceName)} ↗</a>`:`<p class="notice">기준일이 확인된 구성종목·비중 자료는 아직 확보하지 못했어요.</p><p>${e.category==='cash'?'합성 ETF는 담보 자산만 보고 투자 대상을 판단하기 어려워요. 비교지수와 장외파생상품 계약을 함께 확인해요.':e.category==='commodity'?'선물 ETF는 기업 목록보다 선물 계약의 만기와 교체 방식을 살펴봐요.':e.category==='bonds'?'채권은 발행 주체·만기·신용도와 편입 비중을 함께 살펴봐요.':'공식 자료의 구성종목과 상위 종목 비중을 함께 확인해요. 같은 지수를 따르는 다른 ETF의 비중을 이 상품의 비중으로 대신하지 않습니다.'}</p><a href="${url(e.sourceUrl)}" target="_blank" rel="noopener noreferrer">공식 상품 자료에서 구성 확인 ↗</a>`}</section>`;
}
function detailGuide(e){
 const g=R.productGuide(e);
 return `<section class="panel section"><h2>이 상품, 세 가지로 이해하기</h2><div class="feature-grid"><div><small>투자 유형</small><strong>${esc(groups[e.group]||categories[e.category])}</strong></div><div><small>투자 시장</small><strong>${esc({us:'미국',korea:'국내',global:'글로벌'}[e.region]||e.region)}</strong></div><div><small>추종지수</small><strong>${esc(e.benchmark)}</strong></div></div><details class="detail-reading"><summary>상품 특성과 위험 자세히 읽기</summary><h3>무엇에 투자하나요?</h3><p>${esc(g.target)}</p><h3>가격에 영향을 주는 요인</h3><p>${esc(g.drivers)}</p><h3>매수 전에 확인할 것</h3><p>${esc(g.check)}</p><p class="muted">상품 구조에 대한 설명이며 오늘의 등락 원인을 분석한 결과는 아닙니다.</p><a href="#/learn/${g.lesson}">관련 개념 배우기 →</a></details><details class="detail-reading" id="evidence"><summary>설명의 근거자료 확인하기</summary><p><a href="${url(e.sourceUrl)}" target="_blank" rel="noopener noreferrer">상품 공식 자료 ↗</a> · 투자 대상·추종지수·상품 구조</p><p><a href="https://finance.naver.com/item/main.naver?code=${e.ticker}" target="_blank" rel="noopener noreferrer">NAVER 가격 자료 ↗</a> · 종가·거래량</p><dl>${['expenseRatio','aum','inceptionDate'].map((key,i)=>`<div><dt>${['총보수','순자산','상장일'][i]}</dt><dd>${fundamental(e,key)}</dd></div>`).join('')}</dl><p class="muted">각 자료의 기준일과 확인일을 함께 살펴보세요.</p></details></section>`;
}

function dataAvailability(e){
 const history=e.distributionHistory;
 return `<section class="panel section"><h2>어디까지 확인된 자료인가요?</h2><dl><div><dt>가격·거래량</dt><dd>${esc(e.asOf)} 기준 · NAVER 종가<small>실시간 시세가 아닙니다.</small></dd></div>${['expenseRatio','aum','inceptionDate'].map((key,i)=>`<div><dt>${['총보수','순자산','상장일'][i]}</dt><dd>${fundamental(e,key)}</dd></div>`).join('')}<div><dt>분배금</dt><dd>${history?`일부 ${e.distributions.length}건 확인<small>지급 기준일 ${e.distributions[0].recordDate} ~ ${e.distributions.at(-1).recordDate}</small><small>확인 ${history.checkedAt} · <a href="${url(history.sourceUrl)}" target="_blank" rel="noopener noreferrer">공식 지급 내역 ↗</a></small>`:'미확보 · 지급액이 0원이라는 뜻이 아닙니다.'}</dd></div><div><dt>NAV·추적오차</dt><dd>미확보 · 계산하지 않습니다.</dd></div></dl><p class="muted">가격 갱신은 보수·순자산·분배금 확인일을 갱신하지 않습니다. 과거 자료를 현재 값으로 판단하지 말고 공식 자료를 다시 확인해 주세요.</p></section>`;
}
function similarProducts(e){
 const list=R.peers(e,funds);
 return `<section class="panel section"><h2>같은 지수의 ETF 비교하기</h2><p>추종지수·자산 종류·지역·전략·상품유형이 같은 등록 상품만 연결합니다. 성과 순위가 아닙니다. 총보수·규모·자료 기준일을 확인한 뒤 두 상품을 비교해요. 총보수는 전체 투자 비용과 다르며, 기준일이 다른 수치로 우열을 판단하지 않아요.</p>${list.length?list.map(x=>`<div class="peer-row"><div><a href="#/etf/${x.ticker}">${esc(x.name)}</a><small> · ${esc(x.issuer)}</small><p>같은 투자 기준: ${esc(x.benchmark)}</p><dl><div><dt>총보수</dt><dd>${fundamental(x,'expenseRatio')}</dd></div><div><dt>순자산</dt><dd>${fundamental(x,'aum')}</dd></div></dl></div><button class="secondary" data-peer="${e.ticker}:${x.ticker}" aria-label="${esc(e.name)}과 ${esc(x.name)} 비교">이 상품과 비교 →</button></div>`).join(''):'<p class="notice">현재 등록 목록에는 같은 기준의 다른 상품이 없습니다. 시장에 비교 상품이 없다는 뜻은 아닙니다.</p><a href="#/explore">전체 등록 상품 둘러보기 →</a>'}${list.length?'<p class="muted">버튼을 누르면 기존 비교 목록을 이 두 상품으로 바꿉니다.</p>':''}</section>`;
}
function comparePair(value){
 const [first,second]=String(value).split(':'),e=funds.find(x=>x.ticker===first);
 if(!e||!R.peers(e,funds).some(x=>x.ticker===second))return false;
 selected=new Set([first,second]);location.hash='#/compare';return true;
}
function learn(){return heading('SURF SCHOOL','처음이라 궁금한 것들','상품을 고르기 전에, 한 가지씩 이해해 봐요.')+`<div class="learn-grid">${terms.map(([id,t,b],i)=>`<article class="panel" id="term-${id}"><p class="eyebrow">LESSON ${String(i+1).padStart(2,'0')}</p><h2>${t}</h2><p>${b}</p></article>`).join('')}</div><p class="notice">상품별 위험은 공식 설명서를 확인하세요. <a href="#/method">이 사이트의 출처와 계산 기준 →</a></p>`}
function method(){return heading('OUR COMPASS','기준을 숨기지 않아요','무엇을 추천하고, 어디까지 확인했는지 공개합니다.')+`<section class="panel"><h2>후보는 이렇게 좁혀요</h2><ol><li>1년 이내 사용할 자금 또는 손실을 받아들이기 어려운 답변이면 후보를 제시하지 않습니다.</li><li>변동이 부담스럽다는 답변이면 등록된 국내 국고채 ETF의 구조를 먼저 안내합니다. 다른 선호보다 이 답변을 우선합니다.</li><li>분배금 관심은 배당 전략, 그 외에는 대표지수 주식 ETF로 좁히고 관심 시장을 반영합니다.</li><li>금 선물·산업 집중·나스닥100·단기채·금리형은 직접 탐색할 수 있지만 현재 설문 규칙에서는 자동 후보에 포함하지 않습니다.</li></ol><p>이 규칙은 프로젝트의 학습용 분류 기준입니다. 1년·3년 구간이 안전성을 보장하는 금융 기준은 아니며, 수익률 예측·개별 적합성 판정·투자 비중 제안은 제공하지 않습니다.</p><p>결과는 종목코드 순서입니다. 수익률·보수 순위를 매기거나 광고·제휴에 따라 상품을 우선하지 않습니다.</p></section><section class="panel section"><h2>자료와 계산의 범위</h2><p>NAVER 일별 종가·거래량의 저장 자료를 사용합니다. 가격은 ${funds[0]?.asOf||'미확보'} 기준이며 평일 한국시간 19:23 예약 갱신 설정이 있으며, 실제 실행 상태는 가격 안내와 갱신 기록에서 확인할 수 있습니다. 기본 정보는 2026-09-24~25에 수동 확인한 운용사·거래소 자료로, 각 항목에 원래 기준일을 표시합니다.</p><p>상품별 가격 이력을 따로 보관하며 비교는 선택한 상품 모두에 있는 거래일만 사용합니다. 최근 1년은 공통 마지막 관측일에서 달력상 12개월 전 이하의 가장 가까운 관측 종가를 기준으로 합니다. 수익률 = 마지막 가격 ÷ 시작 가격 − 1. 최대낙폭 = 각 가격 ÷ 그때까지 최고 가격 − 1의 최솟값입니다.</p><p>분배금 재투자, 세금, 거래비용은 별도 계산하지 않습니다. 가격 조정 이력은 독립 검증하지 않아 경제적 총수익률로 해석할 수 없습니다. NAV·지수 시계열이 없어 추적오차도 계산하지 않습니다.</p><p>보수·순자산의 자료 날짜는 상품마다 다르고 일부는 수년 전 자료입니다. 현재 값은 링크된 공식 자료에서 다시 확인하세요. 미확보를 0으로 대체하지 않습니다.</p><p>답변과 비교 목록은 현재 페이지의 메모리에서만 처리합니다. 로그인·서버 저장·분석 추적 도구가 없습니다.</p></section><section class="panel section"><h2>상품별 공식 출처</h2>${funds.map(e=>`<p><a href="${url(e.sourceUrl)}" target="_blank" rel="noopener noreferrer">${esc(e.name)} ↗</a> · <a href="#/etf/${e.ticker}">항목별 기준일 확인</a></p>`).join('')}</section>`}
function render(focus=true){const [,route='',arg='']=(location.hash||'#/').split('/');if(route==='explore'&&arg){category=categories[arg]?arg:'all';group=groups[arg]?arg:'all';region='all';query='';}main.innerHTML=({guide,result,explore,compare,learn,method}[route]|| (route==='etf'?()=>detail(arg):route===''?home:()=>heading('NOT FOUND','페이지를 찾지 못했어요')+'<a href="#/">홈으로 돌아가기</a>'))();document.getElementById('count').textContent=selected.size;document.querySelectorAll('nav a').forEach(a=>{if(a.hash==='#/'+route)a.setAttribute('aria-current','page');else a.removeAttribute('aria-current')});if(focus)main.focus({preventScroll:true});if(route==='learn'&&arg){document.getElementById('term-'+arg)?.scrollIntoView()}else if(focus)window.scrollTo(0,0);}
let toastTimer;function notify(text){clearTimeout(toastTimer);document.getElementById('toast').textContent=text;toastTimer=setTimeout(()=>document.getElementById('toast').textContent='',3500)}
main.addEventListener('click',event=>{const view=event.target.closest('[data-view]');if(view){catalogView=view.dataset.view==='returns'?'returns':'basic';const y=window.scrollY;render(false);window.scrollTo(0,y);main.querySelector(`[data-view="${catalogView}"]`)?.focus({preventScroll:true});return;}if(event.target.closest('[data-evidence]')){event.preventDefault();document.getElementById('evidence')?.scrollIntoView();return;}const peer=event.target.closest('[data-peer]');if(peer){comparePair(peer.dataset.peer);return;}const b=event.target.closest('[data-select]');if(!b)return;const id=b.dataset.select;if(!funds.some(e=>e.ticker===id))return;if(selected.has(id))selected.delete(id);else if(selected.size<3)selected.add(id);else return notify('최대 3개까지 비교할 수 있어요. 먼저 하나를 제외해 주세요.');const y=window.scrollY;render(false);window.scrollTo(0,y);main.querySelector(`[data-select="${id}"]`)?.focus({preventScroll:true});notify(`${selected.size}개 ETF를 비교에 담았어요.`)});
function showScenario(data){const out=document.getElementById('scenario-result');if(!out)return;try{const e=funds.find(x=>x.ticker===data.ticker),s=R.scenario(e,{monthly:Number(data.monthly),months:Number(data.months)});if(!s){out.innerHTML='<p class="notice warning">선택한 기간의 가격 이력이 부족합니다.</p>';return}out.innerHTML=`<div class="scenario-output"><h3>${esc(e.name)} · 과거 ${s.monthCount}회 납입</h3><dl><div><dt>누적 투자금</dt><dd>${F.money(s.invested)}</dd></div><div><dt>마지막 평가금액</dt><dd>${F.money(s.value)}</dd></div><div><dt>손익</dt><dd>${F.money(s.profit)} (${F.percent(s.return)})</dd></div><div><dt>시나리오 최대낙폭</dt><dd>${F.percent(s.mdd)}</dd></div><div><dt>보유 수량 · 잔여 현금</dt><dd>${F.number(s.units)}좌 · ${F.money(s.cash)}</dd></div></dl><p class="muted">계산 기간 ${s.start} ~ ${s.end}. 첫 달 첫 관측 거래일부터 매월 첫 관측 거래일 종가로 매수합니다. 같은 금액을 과거에 투자했다는 가상 계산이며 실제 체결 내역이 아닙니다.</p></div>`}catch(e){out.innerHTML=`<p class="notice warning">${esc(e.message)}</p>`}}
main.addEventListener('submit',event=>{event.preventDefault();const data=Object.fromEntries(new FormData(event.target));if(event.target.id==='guide'){try{R.recommend(data,funds);answers=data;location.hash='#/result'}catch(e){notify(e.message)}}else if(event.target.id==='filters'){category=data.category in categories?data.category:'all';group=data.group in groups?data.group:'all';region=['korea','us','global'].includes(data.region)?data.region:'all';query=data.query.trim();history.replaceState(null,'','#/explore');render(false)}else if(event.target.id==='scenario')showScenario(data)});
window.addEventListener('hashchange',()=>render());render(false);
